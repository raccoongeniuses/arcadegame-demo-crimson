A dashboard portal created using Vite (Vue 3), daisyUI and Tailwind CSS

Built using Node.js 20.15.0, for other Node versions use Docker:

docker compose up -d --build (in root folder arcadegames-demo) for building the image containers (MySQL 8, Frontend Vue + Vite and Backend Express + Sequelize)





